{
  "name": "pg-synonyms-api-dev",
  "version": "1.0.0",
  "description": "Dev API (light) for PagineGialle to integrate with n8n",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "cli": "node server.js --cli",
    "postinstall": "PUPPETEER_CACHE_DIR=.cache/puppeteer npx puppeteer browsers install chrome --browser-revision=127.0.6533.88"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer": "^22.12.1",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
